# Scripts

No external download script was executed in this shell session because the external site was not reachable from the local command environment.

If network access becomes available later, the inventory in `../url_inventory.csv` can be used to drive a focused downloader for only CCASS-related pages.

