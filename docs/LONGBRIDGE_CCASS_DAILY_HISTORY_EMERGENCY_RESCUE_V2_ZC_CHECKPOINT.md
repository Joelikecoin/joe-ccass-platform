# LONGBRIDGE_CCASS_DAILY_HISTORY_EMERGENCY_RESCUE_V2 — ZC CHECKPOINT

> Package `LONGBRIDGE_CCASS_DAILY_HISTORY_EMERGENCY_RESCUE_V2_ZC`, executor ZC,
> baseline commit `8e9b7d9`. Production authority: Codex (untouched).
> Note: the referenced `docs/LONGBRIDGE_CCASS_DAILY_HISTORY_EMERGENCY_RESCUE_V2.md`
> and coverage V1.json do not exist on this machine (home-machine artifacts) — this
> checkpoint is created fresh; no prior discoveries were discarded.

## Verified runtime state (this machine, LIVE, 2026-09-25)

```text
DAILY_HISTORY_SURFACE_EXISTS=YES
700.HK A00003: 40 rows, 2026.07.31 → 2026.09.24   (exact package proof reproduced)
700.HK B01955: 40 rows, 2026.07.31 → 2026.09.24   (exact package proof reproduced)
SYMBOL_NORMALIZATION_RULE: strip leading zeros + ".HK"
  (live samples: 1/3/5/6/2318/3888/700/9618.HK all return data; "00700.HK" not assumed)
broker_holding_detail participant id field = `parti_number`
broker_holding_daily return = {"list":[{date "2026.09.24", holding, ratio, chg}]}
```

## Engine (committed)

- `app/rescue/longbridge_daily_rescue.py`: RescueStore (raw SQLite, natural key
  stock+participant+date, idempotent skip, conflict preservation, error/empty
  classification, checkpoint table), rescue_stock(), universe builder, anchor
  filter (keep 2026-07-31 overlap, drop pre-window).
- `scripts/zc_run_longbridge_rescue.py`: runner; universe = production Turso
  `stocks` table (**68 codes**, priority 3; priority-1 extension universe is a
  home-machine asset, honestly recorded as unavailable here).
- Rescue store: `08_DATA_ASSETS/longbridge_rescue/longbridge_ccass_daily_rescue_20260925.sqlite`
  (Drive; falls back to repo `data/longbridge_rescue/` offline).
- Tests: `tests/test_longbridge_daily_rescue.py` — 10 passed, including the two
  EXACT_700 live proofs and mock coverage of idempotency/conflict/checkpoint/
  empty-classification/anchor/lineage.

## Trial evidence (2 stocks, before full run)

```text
TOTAL_ROWS=7498  DISTINCT_STOCKS=2  DISTINCT_PARTICIPANTS=165
DISTINCT_DATES=40  EARLIEST=2026-07-31  LATEST=2026-09-24
AUG_01_TO_SEP_08_ROWS=5057  ← suspected gap is NOT zero-data (§15 confirmed)
CONFLICTS=0  ERRORS=0
```

## Empty-response discipline (§9)

[] is never stored as zero; every empty participant query is recorded in
`errors` as `EMPTY:{kind}` with kinds VALID_EMPTY_FOR_THIS_PARTICIPANT /
INVALID_SYMBOL_FORMAT / INVALID_PARTICIPANT_ID / RUNTIME/API_EMPTY / UNKNOWN.
Detail-listed participants classify differently from unknown ids.

## Transient participant risk (§7)

`POTENTIAL_TRANSIENT_PARTICIPANT_GAP=UNKNOWN/PARTIAL` — completeness is claimed
only over the participant union actually enumerated (current detail + extra
union ids). Not resolved by this run.

## Production isolation (§22)

Rescue store is isolated; no writes to Turso canonical tables, Webb source,
extension source, or precedence. Codex handoff = read-only reconciliation.

## Run status

See `rescue_readback_20260925.json` next to the store after completion, and
`docs/LONGBRIDGE_DAILY_CCASS_RESCUE_COVERAGE_V2.json` (generated by the
coverage step of the final report).


## WRAP-UP STATE (2026-09-25, company machine session end — resume on home machine)

```text
MASTER_STORE=08_DATA_ASSETS/longbridge_rescue/longbridge_ccass_daily_rescue_20260925.sqlite
TOTAL_ROWS=49454  DISTINCT_STOCKS=5  DISTINCT_PARTICIPANTS=470
DISTINCT_DATES=40  EARLIEST=2026-07-31  LATEST=2026-09-24
AUG_01_TO_SEP_08_ROWS=33,340 (5 stocks) — gap confirmed RECOVERED, not zero-data
CONFLICTS=0  ERRORS=2 (both EMPTY classifications)
COVERAGE_MAP=docs/LONGBRIDGE_DAILY_CCASS_RESCUE_COVERAGE_V2.json (regenerated at wrap-up)
RESCUED_STOCKS=00001,00003,00005 + 00002,00004 (batchA partial, merged idempotently)

SCALING_FINDINGS (FULL_UNIVERSE_RESCUE_SCALING_CHECK_V1, partial):
- extension universe = 3,000 codes (2026-07 month file) → all mappable via strip-zeros+.HK
- participant bridge: participants.csv partID→ccassID = Longbridge broker_id (1,750 ids)
- extension-set pruning REJECTED (would lose 39.8-45.4% of rows)
- detail-based pruning = no-op (0% loss, no savings)
- measured: ~10-18 participants/min/worker; big stocks 30-45 min at concurrency=2
- full 2,997 remaining ≈ multi-day continuous operation (concurrency 2 → ~300h; 8 → ~75h if safe)
- concurrency=2 observed: no auth errors, no 429, empty-rate ~0.2% baseline — pending batch gates

RESUME INSTRUCTIONS (home machine):
1. copy/ensure the rescue store (or recompute from this artifact) + the two data/ jsons
   (extension_universe_2026_07.json, extension_participants_2026_07_ccassids.json —
   regenerable from the extension zip via the same one-pass scripts)
2. python -m scripts.zc_run_longbridge_rescue_scaled --universe data/extension_universe_2026_07.json
   --batch 10 --concurrency 2 --tag batchA   (auto-skips the 5 already-rescued stocks)
3. gates per §5-7, then BATCH_B(25) → BATCH_C(50) → full expansion; never redo completed stocks
4. worker leftover files (worker_batchA_*.sqlite) on G: are MERGED already; delete on next visit
```
